import {Selector} from 'react-redux';
import {RootStoreState} from '@shared-state';
import { SettingReduxSelectionState} from './types';

export const SettingSelector: Selector<RootStoreState, SettingReduxSelectionState> = (
  state,
) => {
  return {};
};
